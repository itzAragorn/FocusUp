<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FocusUp" parent="android:Theme.Material.NoActionBar">
        <!-- Colores primarios -->
        <item name="android:colorPrimary">@color/electric_purple</item>
        <item name="android:colorPrimaryDark">@color/electric_purple_dark</item>
        <item name="android:colorAccent">@color/electric_purple</item>
        
        <!-- Colores de fondo -->
        <item name="android:colorBackground">@color/dark_graphite</item>
        <item name="android:windowBackground">@color/dark_graphite</item>
        
        <!-- Colores de texto -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        
        <!-- Barra de estado -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Barra de navegación -->
        <item name="android:navigationBarColor">@color/background_dark</item>
    </style>
</resources>